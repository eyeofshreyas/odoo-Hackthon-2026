/**
 * ProtectedRoute
 * Redirects unauthenticated users to /login.
 * Wraps routes that require a valid JWT session.
 * TODO: Implement in Phase 3
 */
// placeholder
const ProtectedRoute = ({ children }) => children;
export default ProtectedRoute;
