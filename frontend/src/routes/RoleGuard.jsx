/**
 * RoleGuard
 * Blocks access to routes for unauthorized roles (returns 403 view).
 * Accepts llowedRoles prop and checks against AuthContext.
 * TODO: Implement in Phase 3
 */
// placeholder
const RoleGuard = ({ children }) => children;
export default RoleGuard;
